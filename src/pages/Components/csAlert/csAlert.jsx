import React, { Component } from "react";
import { Alert } from "react-bootstrap";
import "./csalert.css";

class csAlert extends Component {
  render() {
    return (
      <React.Fragment>
        <Alert>Alert</Alert>
      </React.Fragment>
    );
  }
}

export default csAlert;
